# Forward Hub (Cloudflare Workers + KV + D1)

一个完全免费、无需 R2、可直接部署到 Cloudflare 的 Forward 模块托管服务。

支持：
- 上传 Forward 模块（≤5MB）
- 自动存储到 KV
- 自动写入 D1 数据库
- 列出所有模块
- 获取模块内容
- 删除模块
- 自动生成订阅链接

---

## 🚀 部署步骤

### 1. Fork 或 Clone 本仓库

