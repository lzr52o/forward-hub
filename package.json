{
  "name": "forward-hub",
  "version": "1.0.0",
  "description": "A lightweight Forward widget hub using Cloudflare Workers, KV and D1.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'Build skip'"
  },
  "devDependencies": {
    "wrangler": "^3.109.0"
  }
}
