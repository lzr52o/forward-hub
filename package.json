{
  "name": "forward-hub",
  "version": "1.0.0",
  "main": "src/index.ts", 
  "type": "module",
  "scripts": {
    "build": "echo 'Using Wrangler internal bundler'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
